#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;

int main(void)
{
    int n;
    int tmp;
    scanf("%d", &n);
    vector<int> arr;
    for(int i=0;i<n;i++){
        scanf("%d", &tmp);
        arr.push_back(tmp);
    }
    
    sort(arr.begin(), arr.end());

	for (int i = 0; i < n; i++)
		printf("%d\n", arr[i]);
}